#include "main.h"

/**
 * mul - prints a test that multiply two int
 * @a: The first int
* @b: The second int
 * Return: the result of multiplication of int a and b
 */

int mul(int a, int b)
{
return (a * b);
}
